// FILE LOCATION: quantai/packages/shared-types/index.js
//
// Placeholder for Phase 0. Populated starting Phase 2 (Auth) and Phase 3
// (Portfolios) with shared shapes used by both apps/web and apps/api,
// e.g. Portfolio, Holding, User (without sensitive fields), etc.
// Kept as plain JS objects/JSDoc for now since apps/api stays JavaScript;
// revisit if either app moves to TypeScript later.

module.exports = {};
